# Mini Facturador — Svelte 5 (STARTER)

Punto de partida del live coding del Día A. Ya trae lo que **no** se escribe en
clase: `src/lib/types.ts` y `src/lib/calculos.ts` (reutilizados del Facturador,
sin cambios) y `public/productos.json`.

Los componentes (`App.svelte`, `Catalogo.svelte`, `Carrito.svelte`) están como
esqueletos con marcas 🟢 que se completan siguiendo la guía.

## Arrancar
```bash
npm install
npm run dev
```

> ⚠️ Siempre con `npm run dev` (Vite). No abrir `index.html` directo ni con Live Server.

## Caso B (respaldo)
Si tu propia versión se rompió durante la clase, clona este starter y continúa
desde aquí. (Penalización −1 según rúbrica.)
